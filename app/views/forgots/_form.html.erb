<%= form_for(@forgot) do |f| %>
  <% if @forgot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forgot.errors.count, "error") %> prohibited this forgot from being saved:</h2>

      <ul>
      <% @forgot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
